// UI => Action => Reducer => store
export const CategoryReducer = (state, action) => {

    switch(action.type){
        
        default:
            return {
                ...state
            }
    }
}